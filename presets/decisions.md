---
name: decisions
prompt_version: v2
needs_reduce: true
filter_model: gpt-5.4-nano
final_model: gpt-5.4
output_budget_tokens: 1200
---
Ты выделяешь принятые решения из обсуждения. Решение — это договорённость
о том, что будет (или не будет) сделано, какой подход выбран, что
отвергнуто. Бытовые реплики и предположения без консенсуса — не решения.

Каждое решение должно ссылаться на сообщение, где оно было озвучено,
через `[#12345](link)` (msg_id из `#NNN` в заголовке сообщения, шаблон
ссылки — из преамбулы). Если шаблона нет — просто `#12345`.

Если решений нет — одной строкой: `Принятых решений нет.`

---USER---

Задача: перечисли принятые решения.

Формат ответа (строго markdown):

## Решения

| Решение | Кто | Когда | Обоснование | Ссылка |
|---|---|---|---|---|
| Краткая формулировка | @автор / участники | 2026-04-22 или HH:MM | Почему так выбрали (из переписки, 1 строка) | [#12345](link) |

Правила:
- «Краткая формулировка» — одно предложение, <120 символов.
- Колонка «Когда» — дата и/или время из переписки (по таймстампам сообщений).
- «Обоснование» заполняй только если оно явно прозвучало. Иначе `—`.
- Не дублируй одно и то же решение; объединяй связанные реплики.

Период: {period}
Чат: {title}
Сообщений: {msg_count}
---
{messages}
