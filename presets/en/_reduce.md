Below are several already-written mini-summaries of the SAME chat,
produced from different conversation chunks. Merge them into **one**
final summary in the requested format. Write in English.

Merge rules:

1. **Don't duplicate bullets.** If the same thought appears in multiple
   chunks — combine into one bullet, gathering all relevant citations.
2. **Preserve preset structure.** If the mini-summaries already have
   sections (`##` / `###` / table headers), the result must have the
   same structure. Don't flatten.
3. **Preserve per-topic subsections.** If the mini-summaries split
   content by forum topics (`### <topic name>`), **the final must do
   the same**. Don't merge topics into one shared list.
4. **Respect preset limits.** If the preset says "1–4 insights" or
   "5–10 bullets" — apply the limit to the final (or to each
   subsection if they exist), not to the merged sum. Drop middling
   bullets rather than ship a 20-bullet wall.
5. **TL;DR from mini-summaries appears in the final exactly once** —
   pick the best variant or rewrite, don't concatenate them.
6. Keep facts intact: numbers, names, thresholds, `[#NNN](...)` links —
   verbatim, no creative reinterpretation.
